<div class="row">
    <div class="col-sm-4 col-lg-3">
        <!-- Menu Category -->
        {{ include('repository_templates/menu_categories.html.twig') }}

        <!-- Side-widget LastVendors -->
        {{ render(controller(
            'App\\Controller\\RepositoryController::last_vendors'
        )) }}
    </div>

    <div class="col-sm-8 col-lg-9">
        <!-- Main - Cards -->
        {{ include('inc/vendor-list-cards-home.html.twig') }}
    </div>
</div>