<div class="col-sm-4 col-lg-3">
    <div class="sidebar">
        <div class="widget">
            <h2 class="widgettitle">Categories</h2>
            <ul class="menu">
                {% for categorie in categories %}
                    <li><a href="{{ path('category', { id : categorie.id }) }}">{{ categorie.Name }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>